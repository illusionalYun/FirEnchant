# 配置文件版本, 勿动捏 ~
file-version: 1

# 语言配置, 请确保语言缩写是Locale可识别的, 如 en, zh_CN, zh_TW 等.
language: "zh-CN"

# 数据库
database:
  # 数据库类型 (mysql h2)
  type: "h2"
  mysql:
    host: "127.0.0.1:3306"
    database: "firenchant"
    user: "root"
    password: "root"
  h2:
    file: "database.db"